---
cssclasses:
  - dashboard
---

# DM Dashboard

## Al Tavolo

```meta-bind-button
label: Durante Il Gioco
style: primary
actions:
  - type: open
    link: "[[Durante il Gioco]]"
```

```meta-bind-button
label: Prossima Sessione
style: primary
actions:
  - type: open
    link: "[[Risorse/Preparazione Sessione]]"
```

```meta-bind-button
label: Preparazione Sessioni
style: primary
actions:
  - type: open
    link: "[[z.bacheche/Preparazione Sessioni]]"
```

```meta-bind-button
label: Post Sessione
style: primary
actions:
  - type: open
    link: "[[z.bacheche/Post Sessione]]"
```

```meta-bind-button
label: Inbox
style: primary
actions:
  - type: open
    link: "[[Inbox/Inbox]]"
```

```meta-bind-button
label: Guida DM
style: primary
actions:
  - type: open
    link: "[[Risorse/Guida DM]]"
```

```meta-bind-button
label: Worldbuilder
style: primary
actions:
  - type: open
    link: "[[Worldbuilder Dashboard]]"
```

## Crea Per Il Tavolo

```meta-bind-button
label: Nuova Campagna
style: primary
actions:
  - type: templaterCreateNote
    templateFile: "z.modelli/dm/Campagna.md"
    folderPath: "Campagne"
    open: true
```

```meta-bind-button
label: Nuova Sessione
style: primary
actions:
  - type: templaterCreateNote
    templateFile: "z.modelli/dm/Sessione.md"
    folderPath: "Mondi/Sessioni"
    open: true
```

```meta-bind-button
label: Nuova Missione
style: primary
actions:
  - type: templaterCreateNote
    templateFile: "z.modelli/dm/Missione.md"
    folderPath: "Mondi/Missioni"
    open: true
```

```meta-bind-button
label: Nuovo Incontro
style: primary
actions:
  - type: templaterCreateNote
    templateFile: "z.modelli/dm/Incontro.md"
    folderPath: "Mondi/Incontri"
    open: true
```

## Stato Rapido

```dataviewjs
const count = (source, predicate) => dv.pages(source).where(predicate).length;
const escapeHtml = value => String(value ?? "").replace(/[&<>"']/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", "'": "&#39;" }[c]));

const cards = [
  ["Campagne", count('"Campagne"', p => p.file.name !== "Campagne" && p.stato !== "archiviata"), "Attive o da seguire"],
  ["Sessioni pronte", count('"Mondi/Sessioni"', p => p.stato === "pronto"), "Da portare al tavolo"],
  ["Missioni aperte", count('"Mondi/Missioni"', p => ["proposta", "accettata", "in corso", "pronto"].includes(p.stato)), "Scelte vive"],
  ["Materiale pronto", count('"Mondi/Incontri" OR "Mondi/Dispense" OR "Mondi/Oggetti"', p => p.stato === "pronto"), "Usabile subito"],
  ["Idee da smistare", count('"Inbox"', p => p.file.name !== "Inbox" && !["smistata", "archiviata"].includes(p.stato)), "Appunti da decidere"]
];

const grid = dv.el("div", "", { cls: "gdr-stat-grid" });
grid.innerHTML = cards.map(([label, value, hint]) => `
  <div class="gdr-stat-card">
    <div class="gdr-stat-value">${escapeHtml(value)}</div>
    <div class="gdr-stat-label">${escapeHtml(label)}</div>
    <div class="gdr-stat-hint">${escapeHtml(hint)}</div>
  </div>
`).join("");
```

## Campagne

```dataview
TABLE stato, tono, livello_attuale, personaggi, luoghi
FROM "Campagne"
WHERE file.name != "Campagne" AND stato != "archiviata" AND stato != "conclusa"
SORT stato ASC, nome ASC
LIMIT 8
```

## Prossime Sessioni

```dataviewjs
const escapeHtml = value => String(value ?? "").replace(/[&<>"']/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", "'": "&#39;" }[c]));
const internalLink = file => `<a class="internal-link" data-href="${escapeHtml(file.path)}" href="${escapeHtml(file.path)}">${escapeHtml(file.name)}</a>`;
const linkList = value => dv.array(value ?? []).map(x => {
  const path = x?.path ?? String(x ?? "");
  const name = x?.display ?? String(x ?? "").split("/").pop();
  return `<a class="internal-link" data-href="${escapeHtml(path)}" href="${escapeHtml(path)}">${escapeHtml(name)}</a>`;
}).join(", ");

const pages = dv.pages('"Mondi/Sessioni"')
  .where(p => ["preparazione", "pronto"].includes(p.stato))
  .sort(p => p.data ?? "9999-99-99", "asc")
  .limit(4);

if (!pages.length) {
  dv.paragraph("Nessuna sessione in preparazione.");
} else {
  const grid = dv.el("div", "", { cls: "gdr-card-grid" });
  grid.innerHTML = pages.map(p => `
    <div class="gdr-info-card">
      <div class="gdr-card-title">${internalLink(p.file)}</div>
      <div class="gdr-card-meta">${escapeHtml(p.data ?? "Senza data")} · ${escapeHtml(p.data_mondo ?? "Data mondo non indicata")} · ${escapeHtml(p.stato ?? "senza stato")}</div>
      <div class="gdr-card-line">Luoghi: ${linkList(p.luoghi) || "da scegliere"}</div>
      <div class="gdr-card-line">Personaggi: ${linkList(p.personaggi) || "da scegliere"}</div>
    </div>
  `).join("");
}
```

## Missioni Aperte

```dataviewjs
const escapeHtml = value => String(value ?? "").replace(/[&<>"']/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", "'": "&#39;" }[c]));
const internalLink = file => `<a class="internal-link" data-href="${escapeHtml(file.path)}" href="${escapeHtml(file.path)}">${escapeHtml(file.name)}</a>`;
const maybeLink = value => {
  if (!value) return "";
  const path = value.path ?? String(value);
  const name = value.display ?? String(value).split("/").pop();
  return `<a class="internal-link" data-href="${escapeHtml(path)}" href="${escapeHtml(path)}">${escapeHtml(name)}</a>`;
};
const linkList = value => dv.array(value ?? []).map(x => maybeLink(x)).join(", ");

const pages = dv.pages('"Mondi/Missioni"')
  .where(p => ["proposta", "accettata", "in corso", "pronto"].includes(p.stato))
  .sort(p => p.stato ?? "zzz", "asc")
  .limit(6);

if (!pages.length) {
  dv.paragraph("Nessuna missione aperta.");
} else {
  const grid = dv.el("div", "", { cls: "gdr-card-grid" });
  grid.innerHTML = pages.map(p => `
    <div class="gdr-info-card">
      <div class="gdr-card-title">${internalLink(p.file)}</div>
      <div class="gdr-card-meta">${escapeHtml(p.stato ?? "senza stato")} · Committente: ${maybeLink(p.committente) || "non indicato"}</div>
      <div class="gdr-card-line">Luoghi: ${linkList(p.luoghi) || "nessuno"}</div>
      <div class="gdr-card-line">Ricompense: ${linkList(p.ricompense) || "non indicate"}</div>
    </div>
  `).join("");
}
```

## Materiale Pronto

```dataviewjs
const escapeHtml = value => String(value ?? "").replace(/[&<>"']/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", "'": "&#39;" }[c]));
const internalLink = file => `<a class="internal-link" data-href="${escapeHtml(file.path)}" href="${escapeHtml(file.path)}">${escapeHtml(file.name)}</a>`;
const maybeLink = value => {
  if (!value) return "";
  const path = value.path ?? String(value);
  const name = value.display ?? String(value).split("/").pop();
  return `<a class="internal-link" data-href="${escapeHtml(path)}" href="${escapeHtml(path)}">${escapeHtml(name)}</a>`;
};

const pages = dv.pages('"Mondi/Incontri" OR "Mondi/Dispense" OR "Mondi/Oggetti"')
  .where(p => p.stato === "pronto")
  .sort(p => p.categoria ?? "zzz", "asc")
  .limit(8);

if (!pages.length) {
  dv.paragraph("Nessun materiale segnato come pronto.");
} else {
  const grid = dv.el("div", "", { cls: "gdr-card-grid compact" });
  grid.innerHTML = pages.map(p => `
    <div class="gdr-info-card compact">
      <div class="gdr-card-title">${internalLink(p.file)}</div>
      <div class="gdr-card-meta">${escapeHtml(p.categoria ?? "nota")} · ${escapeHtml(p.tipo ?? "tipo non indicato")}</div>
      <div class="gdr-card-line">Luogo: ${maybeLink(p.luogo) || "non indicato"}</div>
    </div>
  `).join("");
}
```

## Inbox

```dataview
TABLE tipo, stato, collegamenti
FROM "Inbox"
WHERE file.name != "Inbox" AND stato != "smistata" AND stato != "archiviata"
SORT file.ctime DESC
LIMIT 8
```
