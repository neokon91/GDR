{
  "name": "gdr-plugin",
  "version": "0.0.1",
  "private": true,
  "description": "Plugin Obsidian: runtime del vault GDR (viste ```gdr, azioni, creazione, Cruscotto DM).",
  "scripts": {
    "gen:types": "python3 ../Dev/Tools/gen_plugin_types.py",
    "gen:bestiario": "python3 ../Dev/Tools/gen_bestiario.py",
    "typecheck": "tsc --noEmit",
    "build": "npm run gen:types && npm run gen:bestiario && npm run typecheck && node esbuild.config.mjs --prod",
    "dev": "npm run gen:types && npm run gen:bestiario && node esbuild.config.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.23.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.4.0"
  }
}
