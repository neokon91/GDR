id: dataview_blocks
purpose: Blocchi Dataview e DataviewJS riusabili nei template generati.
version: 0.1.0
blocks:
  linked_connections_table:
    type: dataview
    use_for:
      - connessioni
      - world_entity
    code: |-
      TABLE categoria, tipo, stato, pressione, prossima_mossa
      FROM "Mondi"
      WHERE contains(this.connessioni, file.link) OR contains(this.entita_impattate, file.link) OR contains(this.propaga_a, file.link)
      SORT categoria ASC, file.name ASC
    fallback: |-
      | Nota | Categoria | Stato | Pressione | Prossima mossa |
      | --- | --- | --- | --- | --- |
  feedback_creazione:
    type: dataviewjs
    use_for:
      - controllo
      - world_entity
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCreationFeedback(dv);
    fallback: |-
      - Verificare mondo.
      - Verificare uso al tavolo.
      - Verificare player-safe.
      - Verificare almeno due connessioni.
  playable_outline:
    type: dataviewjs
    use_for:
      - session
      - scaletta
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderPlayableOutline(dv, dv.current());
    fallback: |-
      | Blocco | Valore |
      | --- | --- |
      | Apertura |  |
      | Obiettivo |  |
      | Scelta |  |
      | Pressione |  |
      | Materiale pronto |  |
  session_anchor_cards:
    type: dataviewjs
    runtime_view: renderSessionAnchorCards
    use_for:
      - session
      - ancore
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionAnchorCards(dv, dv.current());
    fallback: |-
      | Ancora | Note |
      | --- | --- |
      | Mondo |  |
      | Luoghi |  |
      | Fazioni |  |
      | Missioni |  |
  session_material_cards:
    type: dataviewjs
    runtime_view: renderSessionMaterialCards
    use_for:
      - session
      - materiali
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionMaterialCards(dv, dv.current());
    fallback: |-
      | Materiale | Note |
      | --- | --- |
      | Incontri |  |
      | Dispense |  |
      | Media |  |
  session_map_cards:
    type: dataviewjs
    runtime_view: renderSessionMapCards
    use_for:
      - session
      - mappe
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionMapCards(dv, dv.current());
    fallback: |-
      | Mappa | Uso |
      | --- | --- |
  session_live_cards:
    type: dataviewjs
    runtime_view: renderSessionLiveCards
    use_for:
      - session
      - live
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionLiveCards(dv, dv.current());
    fallback: |-
      | Live | Valore |
      | --- | --- |
      | Scena corrente |  |
      | Decisioni |  |
  session_post_cards:
    type: dataviewjs
    runtime_view: renderSessionPostCards
    use_for:
      - session
      - post-sessione
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionPostCards(dv, dv.current());
    fallback: |-
      | Chiusura | Valore |
      | --- | --- |
      | Conseguenze |  |
      | Recap pubblico |  |
  m7_family_cards:
    type: dataviewjs
    runtime_view: renderM7FamilyCards
    use_for:
      - luogo
      - fazione
      - png
      - relazione
      - tracciato
      - continuita
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderM7FamilyCards(dv, dv.current(), "{{ family }}");
    fallback: |-
      | Controllo | Stato |
      | --- | --- |
  world_impact:
    type: dataviewjs
    use_for:
      - world_entity
      - propagation
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldImpact(dv);
    fallback: |-
      | Blocco | Valore |
      | --- | --- |
      | Prossima mossa |  |
      | Conseguenza potenziale |  |
      | Entita impattate |  |
      | Propaga a |  |
  pressure_list:
    type: dataviewjs
    use_for:
      - dashboard
      - session
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/gdr_views.js"));
      gdr.renderPressureList(dv);
    fallback: |-
      | Nota | Tipo | Stato | Pressione | Prossima mossa |
      | --- | --- | --- | --- | --- |
  session_output:
    type: dataviewjs
    use_for:
      - session
      - post_sessione
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/gdr_views.js"));
      gdr.renderSessionOutput(dv, dv.current());
    fallback: |-
      | Tipo | Voce |
      | --- | --- |
  map_assets:
    type: dataview
    use_for:
      - map_asset
    code: |-
      TABLE tipo, stato, coordinates, luoghi
      FROM "Risorse/Mappe"
      WHERE stato != "archiviata"
      SORT file.name ASC
    fallback: |-
      | Mappa | Tipo | Coordinate | Luoghi |
      | --- | --- | --- | --- |
  atlas_now:
    type: dataviewjs
    runtime_view: renderAtlasNow
    use_for:
      - dashboard
      - atlante
      - mappe
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderAtlasNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Scelta | Stato |
      | --- | --- |
      | Fai adesso | correggi il prossimo buco cartografico |
  atlas_readiness:
    type: dataviewjs
    runtime_view: renderAtlasReadiness
    use_for:
      - dashboard
      - atlante
      - mappe
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderAtlasReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Luoghi |  |
      | Marker |  |
      | Rotte |  |
  atlas_queues:
    type: dataviewjs
    runtime_view: renderAtlasQueues
    use_for:
      - dashboard
      - atlante
      - mappe
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderAtlasQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Azione |
      | --- | --- |
  atlas_surface_links:
    type: dataviewjs
    runtime_view: renderAtlasSurfaceLinks
    use_for:
      - dashboard
      - atlante
      - mappe
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderAtlasSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Atlante con marker | vedere coordinate e layer |
      | Importare mappe | normalizzare asset esterni |
  dnd55_material_pipeline:
    type: dataviewjs
    runtime_view: renderDnd55MaterialPipeline
    use_for:
      - session
      - dnd55
      - materiali
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderDnd55MaterialPipeline(dv);
    fallback: |-
      | Materiale | Mondo | Uso |
      | --- | --- | --- |
      | Incontri | luogo, missione, fazione, sessione | scena pronta |
      | Creature | habitat, luogo, fazione, missione | minaccia o leva |
      | Oggetti | luogo, proprietario, missione, conseguenza | ricompensa o prova |
  combat_readiness:
    type: dataviewjs
    runtime_view: renderCombatReadiness
    use_for:
      - incontro
      - dnd55
      - combattimento
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCombatReadiness(dv);
    fallback: |-
      | Incontro | Creature | Luogo | Aggancio |
      | --- | --- | --- | --- |
  dm_dashboard_now:
    type: dataviewjs
    runtime_view: renderDmDashboardNow
    use_for:
      - dashboard
      - dm
      - cockpit
      - triage
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderDmDashboardNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Fai adesso | prepara, gioca, chiudi o smista |
  dm_dashboard_readiness:
    type: dataviewjs
    runtime_view: renderDmDashboardReadiness
    use_for:
      - dashboard
      - dm
      - cockpit
      - stato
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderDmDashboardReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Sessioni |  |
      | Missioni |  |
      | Materiali |  |
      | Inbox |  |
  dm_dashboard_queues:
    type: dataviewjs
    runtime_view: renderDmDashboardQueues
    use_for:
      - dashboard
      - dm
      - cockpit
      - code
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderDmDashboardQueues(dv);
    fallback: |-
      | Coda | Prossima azione |
      | --- | --- |
  dm_dashboard_surface_links:
    type: dataviewjs
    runtime_view: renderDmDashboardSurfaceLinks
    use_for:
      - dashboard
      - dm
      - cockpit
      - navigazione
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderDmDashboardSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Durante il Gioco | giocare la sessione attiva |
      | Preparazione sessione | rifinire il prossimo tavolo |
  vault_control_now:
    type: dataviewjs
    runtime_view: renderVaultControlNow
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Ripara prima | correggi il blocco che impedisce preparazione, tavolo o post-sessione |
  vault_control_readiness:
    type: dataviewjs
    runtime_view: renderVaultControlReadiness
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Inbox |  |
      | Sessioni |  |
      | Materiali |  |
      | Coerenza |  |
  vault_control_queues:
    type: dataviewjs
    runtime_view: renderVaultControlQueues
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlQueues(dv);
    fallback: |-
      | Nota | Azione |
      | --- | --- |
  vault_control_coherence:
    type: dataviewjs
    runtime_view: renderVaultControlCoherence
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - audit
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlCoherence(dv);
    fallback: |-
      | Controllo | Problema |
      | --- | --- |
  vault_control_surface_links:
    type: dataviewjs
    runtime_view: renderVaultControlSurfaceLinks
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Quality report | vedere problemi ampi |
      | Smistamento bozze | decidere materiale generato |
  quality_report_now:
    type: dataviewjs
    runtime_view: renderQualityReportNow
    use_for:
      - dashboard
      - quality report
      - qualita
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderQualityReportNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Qualita prima | correggi sicurezza pubblica, buco operativo o bozza |
  quality_report_coverage:
    type: dataviewjs
    runtime_view: renderQualityReportCoverage
    use_for:
      - dashboard
      - quality report
      - copertura
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderQualityReportCoverage(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Mondi |  |
      | Missioni |  |
      | Pubblico |  |
  quality_report_operational_gaps:
    type: dataviewjs
    runtime_view: renderQualityReportOperationalGaps
    use_for:
      - dashboard
      - quality report
      - audit
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderQualityReportOperationalGaps(dv);
    fallback: |-
      | Nota | Problema |
      | --- | --- |
  quality_report_public_safety:
    type: dataviewjs
    runtime_view: renderQualityReportPublicSafety
    use_for:
      - dashboard
      - quality report
      - player-safe
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderQualityReportPublicSafety(dv);
    fallback: |-
      | Nota | Rischio |
      | --- | --- |
  quality_report_showcase:
    type: dataviewjs
    runtime_view: renderQualityReportShowcase
    use_for:
      - dashboard
      - quality report
      - screenshot
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderQualityReportShowcase(dv);
    fallback: |-
      | Materiale | Uso |
      | --- | --- |
  quality_report_surface_links:
    type: dataviewjs
    runtime_view: renderQualityReportSurfaceLinks
    use_for:
      - dashboard
      - quality report
      - superfici
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderQualityReportSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Controllo vault | correggere blocchi |
      | Vista giocatori | verificare condivisione |
  generated_drafts_now:
    type: dataviewjs
    runtime_view: renderGeneratedDraftsNow
    use_for:
      - dashboard
      - bozze generate
      - smistamento
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderGeneratedDraftsNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Bozza prima | collega, smista, canonizza o archivia |
  generated_drafts_readiness:
    type: dataviewjs
    runtime_view: renderGeneratedDraftsReadiness
    use_for:
      - dashboard
      - bozze generate
      - stato
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderGeneratedDraftsReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Bozze |  |
      | Pronte |  |
      | Senza aggancio |  |
  generated_drafts_queues:
    type: dataviewjs
    runtime_view: renderGeneratedDraftsQueues
    use_for:
      - dashboard
      - bozze generate
      - code
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderGeneratedDraftsQueues(dv);
    fallback: |-
      | Bozza | Azione |
      | --- | --- |
  generated_drafts_destinations:
    type: dataviewjs
    runtime_view: renderGeneratedDraftsDestinations
    use_for:
      - dashboard
      - bozze generate
      - destinazioni
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderGeneratedDraftsDestinations(dv);
    fallback: |-
      | Bozza | Destinazione |
      | --- | --- |
  generated_drafts_resolved:
    type: dataviewjs
    runtime_view: renderGeneratedDraftsResolved
    use_for:
      - dashboard
      - bozze generate
      - canonizzazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderGeneratedDraftsResolved(dv);
    fallback: |-
      | Nota | Stato |
      | --- | --- |
  generated_drafts_surface_links:
    type: dataviewjs
    runtime_view: renderGeneratedDraftsSurfaceLinks
    use_for:
      - dashboard
      - bozze generate
      - superfici
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderGeneratedDraftsSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Controllo vault | verificare residui |
      | Worldbuilder | collegare al mondo |
  worldbuilder_now:
    type: dataviewjs
    runtime_view: renderWorldbuilderNow
    use_for:
      - dashboard
      - worldbuilding
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldbuilderNow(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Scelta | Stato |
      | --- | --- |
      | Fai adesso | crea o collega il prossimo elemento vivo |
  worldbuilder_readiness:
    type: dataviewjs
    runtime_view: renderWorldbuilderReadiness
    use_for:
      - dashboard
      - worldbuilding
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldbuilderReadiness(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Mancano |  |
      | Pronte |  |
      | Pressioni |  |
  worldbuilder_queues:
    type: dataviewjs
    runtime_view: renderWorldbuilderQueues
    use_for:
      - dashboard
      - worldbuilding
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderWorldbuilderQueues(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Nota | Azione |
      | --- | --- |
  worldbuilder_surface_links:
    type: dataviewjs
    runtime_view: renderWorldbuilderSurfaceLinks
    use_for:
      - dashboard
      - worldbuilding
      - cockpit
      - mappe
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderWorldbuilderSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Codex editabile | correggere stati e collegamenti |
      | Atlante con marker | vedere coordinate e layer |
  worldbuilding_control_now:
    type: dataviewjs
    runtime_view: renderWorldbuildingControlNow
    use_for:
      - dashboard
      - worldbuilding
      - controllo
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldbuildingControlNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Ripara prima | controlla profondita, connessioni, canone e player-safe |
  worldbuilding_control_readiness:
    type: dataviewjs
    runtime_view: renderWorldbuildingControlReadiness
    use_for:
      - dashboard
      - worldbuilding
      - controllo
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldbuildingControlReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Profondita |  |
      | Connessioni |  |
      | Canone |  |
      | Player-safe |  |
  worldbuilding_control_queues:
    type: dataviewjs
    runtime_view: renderWorldbuildingControlQueues
    use_for:
      - dashboard
      - worldbuilding
      - controllo
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderWorldbuildingControlQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Riparazione |
      | --- | --- |
  worldbuilding_control_surface_links:
    type: dataviewjs
    runtime_view: renderWorldbuildingControlSurfaceLinks
    use_for:
      - dashboard
      - worldbuilding
      - controllo
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderWorldbuildingControlSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Codex editabile | correggere campi e connessioni |
      | Controllo canone | stabilizzare verita e rumor |
  canon_control_now:
    type: dataviewjs
    runtime_view: renderCanonControlNow
    use_for:
      - dashboard
      - canone
      - lore
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCanonControlNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Sistema prima | controlla fonte, rumor, contraddizioni e retcon |
  canon_control_readiness:
    type: dataviewjs
    runtime_view: renderCanonControlReadiness
    use_for:
      - dashboard
      - canone
      - lore
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCanonControlReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Canonico |  |
      | Rumor |  |
      | Contraddizioni |  |
      | Retcon |  |
  canon_control_queues:
    type: dataviewjs
    runtime_view: renderCanonControlQueues
    use_for:
      - dashboard
      - canone
      - lore
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderCanonControlQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Decisione |
      | --- | --- |
  canon_control_surface_links:
    type: dataviewjs
    runtime_view: renderCanonControlSurfaceLinks
    use_for:
      - dashboard
      - canone
      - lore
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderCanonControlSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Lore hub | vedere segnali e materiale collegato |
      | Revisione lore | ripulire appunti e rumor |
  economy_now:
    type: dataviewjs
    runtime_view: renderEconomyNow
    use_for:
      - dashboard
      - economia
      - rotte
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderEconomyNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Muovi prima | scegli rotta, risorsa, mercato o buco economico |
  economy_readiness:
    type: dataviewjs
    runtime_view: renderEconomyReadiness
    use_for:
      - dashboard
      - economia
      - rotte
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderEconomyReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Rotte |  |
      | Risorse |  |
      | Mercati |  |
      | Buchi |  |
  economy_queues:
    type: dataviewjs
    runtime_view: renderEconomyQueues
    use_for:
      - dashboard
      - economia
      - rotte
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderEconomyQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nodo | Stato |
      | --- | --- |
  economy_dependency_queues:
    type: dataviewjs
    runtime_view: renderEconomyDependencyQueues
    use_for:
      - dashboard
      - economia
      - rotte
      - dipendenze
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderEconomyDependencyQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Problema |
      | --- | --- |
  economy_surface_links:
    type: dataviewjs
    runtime_view: renderEconomySurfaceLinks
    use_for:
      - dashboard
      - economia
      - rotte
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderEconomySurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Geopolitica | leggere confini e crisi |
      | Motore Mondo Vivo | propagare conseguenze |
  geopolitical_now:
    type: dataviewjs
    runtime_view: renderGeopoliticalNow
    use_for:
      - dashboard
      - geopolitica
      - poteri
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderGeopoliticalNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Muovi prima | scegli territorio, relazione, crisi o buco politico |
  geopolitical_readiness:
    type: dataviewjs
    runtime_view: renderGeopoliticalReadiness
    use_for:
      - dashboard
      - geopolitica
      - poteri
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderGeopoliticalReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Territori |  |
      | Relazioni |  |
      | Crisi |  |
      | Buchi |  |
  geopolitical_queues:
    type: dataviewjs
    runtime_view: renderGeopoliticalQueues
    use_for:
      - dashboard
      - geopolitica
      - poteri
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderGeopoliticalQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Territorio | Pressione |
      | --- | --- |
  geopolitical_pressure_queues:
    type: dataviewjs
    runtime_view: renderGeopoliticalPressureQueues
    use_for:
      - dashboard
      - geopolitica
      - confini
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderGeopoliticalPressureQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Problema |
      | --- | --- |
  geopolitical_surface_links:
    type: dataviewjs
    runtime_view: renderGeopoliticalSurfaceLinks
    use_for:
      - dashboard
      - geopolitica
      - poteri
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderGeopoliticalSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Economia e rotte | leggere risorse e pedaggi |
      | Motore Mondo Vivo | applicare crisi |
  campaign_builder_now:
    type: dataviewjs
    runtime_view: renderCampaignBuilderNow
    use_for:
      - dashboard
      - campagna
      - ambientazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCampaignBuilderNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Trasforma prima | scegli regione, conflitto o cultura da portare a campagna |
  campaign_builder_readiness:
    type: dataviewjs
    runtime_view: renderCampaignBuilderReadiness
    use_for:
      - dashboard
      - campagna
      - ambientazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCampaignBuilderReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Campagne |  |
      | Missioni |  |
      | Sessioni |  |
      | Fronti |  |
  campaign_builder_opportunity_queues:
    type: dataviewjs
    runtime_view: renderCampaignBuilderOpportunityQueues
    use_for:
      - dashboard
      - campagna
      - ambientazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderCampaignBuilderOpportunityQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Fonte | Gancio |
      | --- | --- |
  campaign_builder_campaign_queues:
    type: dataviewjs
    runtime_view: renderCampaignBuilderCampaignQueues
    use_for:
      - dashboard
      - campagna
      - ambientazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderCampaignBuilderCampaignQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Campagna | Stato |
      | --- | --- |
  campaign_builder_surface_links:
    type: dataviewjs
    runtime_view: renderCampaignBuilderSurfaceLinks
    use_for:
      - dashboard
      - campagna
      - ambientazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderCampaignBuilderSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Atlante del mondo | scegliere regione |
      | Preparazione sessione | materializzare prima sessione |
  lore_now:
    type: dataviewjs
    runtime_view: renderLoreNow
    use_for:
      - dashboard
      - lore
      - canone
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLoreNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Segnale prima | scegli canone, mistero, evento o lore da rendere giocabile |
  lore_readiness:
    type: dataviewjs
    runtime_view: renderLoreReadiness
    use_for:
      - dashboard
      - lore
      - canone
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLoreReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Culture |  |
      | Storia |  |
      | Misteri |  |
      | Materiali |  |
  lore_signal_queues:
    type: dataviewjs
    runtime_view: renderLoreSignalQueues
    use_for:
      - dashboard
      - lore
      - canone
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLoreSignalQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Decisione |
      | --- | --- |
  lore_world_queues:
    type: dataviewjs
    runtime_view: renderLoreWorldQueues
    use_for:
      - dashboard
      - lore
      - worldbuilding
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLoreWorldQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Materiale | Uso |
      | --- | --- |
  lore_surface_links:
    type: dataviewjs
    runtime_view: renderLoreSurfaceLinks
    use_for:
      - dashboard
      - lore
      - canone
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLoreSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Atlante | collegare luoghi e mappe |
      | Controllo canone | decidere verita e rumor |
  lore_review_now:
    type: dataviewjs
    runtime_view: renderLoreReviewNow
    use_for:
      - dashboard
      - lore
      - revisione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLoreReviewNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Rivedi prima | completa, collega o rendi giocabile il lore |
  lore_review_readiness:
    type: dataviewjs
    runtime_view: renderLoreReviewReadiness
    use_for:
      - dashboard
      - lore
      - revisione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLoreReviewReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Da completare |  |
      | Non giocabile |  |
      | Isolate |  |
  lore_review_completion_queues:
    type: dataviewjs
    runtime_view: renderLoreReviewCompletionQueues
    use_for:
      - dashboard
      - lore
      - revisione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLoreReviewCompletionQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Serve ancora |
      | --- | --- |
  lore_review_table_queues:
    type: dataviewjs
    runtime_view: renderLoreReviewTableQueues
    use_for:
      - dashboard
      - lore
      - revisione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLoreReviewTableQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Problema |
      | --- | --- |
  lore_review_surface_links:
    type: dataviewjs
    runtime_view: renderLoreReviewSurfaceLinks
    use_for:
      - dashboard
      - lore
      - revisione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLoreReviewSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Controllo canone | decidere verita |
      | Motore Mondo Vivo | applicare effetti |
  living_world_now:
    type: dataviewjs
    runtime_view: renderLivingWorldNow
    use_for:
      - dashboard
      - mondo vivo
      - propagazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLivingWorldNow(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Cambia prima | scegli conseguenza, bersaglio o pressione da aggiornare |
  living_world_readiness:
    type: dataviewjs
    runtime_view: renderLivingWorldReadiness
    use_for:
      - dashboard
      - mondo vivo
      - propagazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLivingWorldReadiness(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Canone vivo |  |
      | Pressioni |  |
      | Propagazioni |  |
      | Buchi |  |
  living_world_queues:
    type: dataviewjs
    runtime_view: renderLivingWorldQueues
    use_for:
      - dashboard
      - mondo vivo
      - propagazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLivingWorldQueues(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Nota | Azione |
      | --- | --- |
  living_world_pressure_queues:
    type: dataviewjs
    runtime_view: renderLivingWorldPressureQueues
    use_for:
      - dashboard
      - mondo vivo
      - pressioni
      - economia
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLivingWorldPressureQueues(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Nodo | Pressione |
      | --- | --- |
  living_world_surface_links:
    type: dataviewjs
    runtime_view: renderLivingWorldSurfaceLinks
    use_for:
      - dashboard
      - mondo vivo
      - propagazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLivingWorldSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Fuori scena | scegliere reazioni tra sessioni |
      | Controllo canone | stabilizzare verita e retcon |
  offscreen_now:
    type: dataviewjs
    runtime_view: renderOffscreenNow
    use_for:
      - dashboard
      - fuori scena
      - reazioni
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderOffscreenNow(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Reagisce prima | scegli attore, clock o conseguenza da muovere |
  offscreen_readiness:
    type: dataviewjs
    runtime_view: renderOffscreenReadiness
    use_for:
      - dashboard
      - fuori scena
      - reazioni
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderOffscreenReadiness(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Attori in moto |  |
      | Clock urgenti |  |
      | Conseguenze aperte |  |
      | Ponte al tavolo |  |
  offscreen_reaction_queues:
    type: dataviewjs
    runtime_view: renderOffscreenReactionQueues
    use_for:
      - dashboard
      - fuori scena
      - reazioni
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderOffscreenReactionQueues(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Nota | Reazione |
      | --- | --- |
  offscreen_table_bridge:
    type: dataviewjs
    runtime_view: renderOffscreenTableBridge
    use_for:
      - dashboard
      - fuori scena
      - preparazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderOffscreenTableBridge(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Materiale | Uso |
      | --- | --- |
  offscreen_surface_links:
    type: dataviewjs
    runtime_view: renderOffscreenSurfaceLinks
    use_for:
      - dashboard
      - fuori scena
      - reazioni
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderOffscreenSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Motore mondo vivo | verificare propagazioni |
      | Preparazione sessione | portare la reazione al tavolo |
  live_table_now:
    type: dataviewjs
    runtime_view: renderLiveTableNow
    use_for:
      - dashboard
      - durante il gioco
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLiveTableNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Gioca adesso | usa scena, scelta, pressione e cattura live |
  live_table_readiness:
    type: dataviewjs
    runtime_view: renderLiveTableReadiness
    use_for:
      - dashboard
      - durante il gioco
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLiveTableReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Luoghi |  |
      | Persone |  |
      | Missioni |  |
      | Materiali |  |
  live_table_queues:
    type: dataviewjs
    runtime_view: renderLiveTableQueues
    use_for:
      - dashboard
      - durante il gioco
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLiveTableQueues(dv);
    fallback: |-
      | Nota | Uso |
      | --- | --- |
  live_table_materials:
    type: dataviewjs
    runtime_view: renderLiveTableMaterials
    use_for:
      - dashboard
      - durante il gioco
      - materiali
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLiveTableMaterials(dv);
    fallback: |-
      | Materiale | Stato |
      | --- | --- |
  live_table_surface_links:
    type: dataviewjs
    runtime_view: renderLiveTableSurfaceLinks
    use_for:
      - dashboard
      - durante il gioco
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLiveTableSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Party control | controllare PG e risorse |
      | Iniziativa | gestire scene a turni |
  table_materials_now:
    type: dataviewjs
    runtime_view: renderTableMaterialsNow
    use_for:
      - dashboard
      - materiali
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderTableMaterialsNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Materiale prima | apri o correggi il materiale che serve alla prossima scena |
  table_materials_readiness:
    type: dataviewjs
    runtime_view: renderTableMaterialsReadiness
    use_for:
      - dashboard
      - materiali
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderTableMaterialsReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Dispense |  |
      | Mappe |  |
      | Incontri |  |
  table_materials_session_queues:
    type: dataviewjs
    runtime_view: renderTableMaterialsSessionQueues
    use_for:
      - dashboard
      - materiali
      - sessione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderTableMaterialsSessionQueues(dv);
    fallback: |-
      | Materiale | Uso |
      | --- | --- |
  table_materials_asset_queues:
    type: dataviewjs
    runtime_view: renderTableMaterialsAssetQueues
    use_for:
      - dashboard
      - materiali
      - asset
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderTableMaterialsAssetQueues(dv);
    fallback: |-
      | Asset | Stato |
      | --- | --- |
  table_materials_dnd_pipeline:
    type: dataviewjs
    runtime_view: renderTableMaterialsDndPipeline
    use_for:
      - dashboard
      - materiali
      - dnd55
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderTableMaterialsDndPipeline(dv);
    fallback: |-
      | Materiale | Gap |
      | --- | --- |
  table_materials_surface_links:
    type: dataviewjs
    runtime_view: renderTableMaterialsSurfaceLinks
    use_for:
      - dashboard
      - materiali
      - superfici
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderTableMaterialsSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Durante il Gioco | usare il materiale |
      | Vista giocatori | controllare cosa mostrare |
  post_session_now:
    type: dataviewjs
    runtime_view: renderPostSessionNow
    use_for:
      - dashboard
      - post sessione
      - chiusura
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderPostSessionNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Chiudi prima | decidi appunti, conseguenze, recap o prossima sessione |
  post_session_readiness:
    type: dataviewjs
    runtime_view: renderPostSessionReadiness
    use_for:
      - dashboard
      - post sessione
      - chiusura
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderPostSessionReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Appunti live |  |
      | Conseguenze |  |
      | Recap |  |
  post_session_closure_queues:
    type: dataviewjs
    runtime_view: renderPostSessionClosureQueues
    use_for:
      - dashboard
      - post sessione
      - canone
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderPostSessionClosureQueues(dv);
    fallback: |-
      | Nota | Decisione |
      | --- | --- |
  post_session_propagation_queues:
    type: dataviewjs
    runtime_view: renderPostSessionPropagationQueues
    use_for:
      - dashboard
      - post sessione
      - conseguenze
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderPostSessionPropagationQueues(dv);
    fallback: |-
      | Bersaglio | Prossima mossa |
      | --- | --- |
  post_session_surface_links:
    type: dataviewjs
    runtime_view: renderPostSessionSurfaceLinks
    use_for:
      - dashboard
      - post sessione
      - superfici
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderPostSessionSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Motore mondo vivo | propagare conseguenze |
      | Vista giocatori | controllare recap pubblico |
  preparation_now:
    type: dataviewjs
    runtime_view: renderPreparationNow
    use_for:
      - dashboard
      - preparazione
      - sessione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderPreparationNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Prepara prima | completa sessione target, ancore e blocchi |
  preparation_readiness:
    type: dataviewjs
    runtime_view: renderPreparationReadiness
    use_for:
      - dashboard
      - preparazione
      - sessione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderPreparationReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Ancore |  |
      | Cinque blocchi |  |
      | Materiali |  |
  preparation_anchor_queues:
    type: dataviewjs
    runtime_view: renderPreparationAnchorQueues
    use_for:
      - dashboard
      - preparazione
      - ancore
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderPreparationAnchorQueues(dv);
    fallback: |-
      | Ancora | Stato |
      | --- | --- |
  preparation_material_queues:
    type: dataviewjs
    runtime_view: renderPreparationMaterialQueues
    use_for:
      - dashboard
      - preparazione
      - materiali
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderPreparationMaterialQueues(dv);
    fallback: |-
      | Materiale | Stato |
      | --- | --- |
  preparation_surface_links:
    type: dataviewjs
    runtime_view: renderPreparationSurfaceLinks
    use_for:
      - dashboard
      - preparazione
      - superfici
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderPreparationSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Durante il Gioco | aprire il tavolo |
      | Materiali al tavolo | verificare output pronti |
contracts:
  dataviewjs_has_no_file_writes: true
  empty_state_required: true
  fallback_required: true
