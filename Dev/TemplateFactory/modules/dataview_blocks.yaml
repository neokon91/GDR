id: dataview_blocks
purpose: Blocchi Dataview e DataviewJS riusabili nei template generati.
version: 0.1.0
blocks:
  linked_connections_table:
    type: dataview
    use_for:
      - connessioni
      - world_entity
    code: |-
      TABLE categoria, tipo, stato, pressione, prossima_mossa
      FROM "Mondi"
      WHERE contains(this.connessioni, file.link) OR contains(this.entita_impattate, file.link) OR contains(this.propaga_a, file.link)
      SORT categoria ASC, file.name ASC
    fallback: |-
      | Nota | Categoria | Stato | Pressione | Prossima mossa |
      | --- | --- | --- | --- | --- |
  feedback_creazione:
    type: dataviewjs
    use_for:
      - controllo
      - world_entity
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCreationFeedback(dv);
    fallback: |-
      - Verificare mondo.
      - Verificare uso al tavolo.
      - Verificare player-safe.
      - Verificare almeno due connessioni.
  playable_outline:
    type: dataviewjs
    use_for:
      - session
      - scaletta
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderPlayableOutline(dv, dv.current());
    fallback: |-
      | Blocco | Valore |
      | --- | --- |
      | Apertura |  |
      | Obiettivo |  |
      | Scelta |  |
      | Pressione |  |
      | Materiale pronto |  |
  session_anchor_cards:
    type: dataviewjs
    runtime_view: renderSessionAnchorCards
    use_for:
      - session
      - ancore
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionAnchorCards(dv, dv.current());
    fallback: |-
      | Ancora | Note |
      | --- | --- |
      | Mondo |  |
      | Luoghi |  |
      | Fazioni |  |
      | Missioni |  |
  session_material_cards:
    type: dataviewjs
    runtime_view: renderSessionMaterialCards
    use_for:
      - session
      - materiali
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionMaterialCards(dv, dv.current());
    fallback: |-
      | Materiale | Note |
      | --- | --- |
      | Incontri |  |
      | Dispense |  |
      | Media |  |
  session_map_cards:
    type: dataviewjs
    runtime_view: renderSessionMapCards
    use_for:
      - session
      - mappe
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionMapCards(dv, dv.current());
    fallback: |-
      | Mappa | Uso |
      | --- | --- |
  session_live_cards:
    type: dataviewjs
    runtime_view: renderSessionLiveCards
    use_for:
      - session
      - live
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionLiveCards(dv, dv.current());
    fallback: |-
      | Live | Valore |
      | --- | --- |
      | Scena corrente |  |
      | Decisioni |  |
  session_post_cards:
    type: dataviewjs
    runtime_view: renderSessionPostCards
    use_for:
      - session
      - post-sessione
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderSessionPostCards(dv, dv.current());
    fallback: |-
      | Chiusura | Valore |
      | --- | --- |
      | Conseguenze |  |
      | Recap pubblico |  |
  m7_family_cards:
    type: dataviewjs
    runtime_view: renderM7FamilyCards
    use_for:
      - luogo
      - fazione
      - png
      - relazione
      - tracciato
      - continuita
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderM7FamilyCards(dv, dv.current(), "{{ family }}");
    fallback: |-
      | Controllo | Stato |
      | --- | --- |
  world_impact:
    type: dataviewjs
    use_for:
      - world_entity
      - propagation
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldImpact(dv);
    fallback: |-
      | Blocco | Valore |
      | --- | --- |
      | Prossima mossa |  |
      | Conseguenza potenziale |  |
      | Entita impattate |  |
      | Propaga a |  |
  pressure_list:
    type: dataviewjs
    use_for:
      - dashboard
      - session
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/gdr_views.js"));
      gdr.renderPressureList(dv);
    fallback: |-
      | Nota | Tipo | Stato | Pressione | Prossima mossa |
      | --- | --- | --- | --- | --- |
  session_output:
    type: dataviewjs
    use_for:
      - session
      - post_sessione
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/gdr_views.js"));
      gdr.renderSessionOutput(dv, dv.current());
    fallback: |-
      | Tipo | Voce |
      | --- | --- |
  map_assets:
    type: dataview
    use_for:
      - map_asset
    code: |-
      TABLE tipo, stato, coordinates, luoghi
      FROM "Risorse/Mappe"
      WHERE stato != "archiviata"
      SORT file.name ASC
    fallback: |-
      | Mappa | Tipo | Coordinate | Luoghi |
      | --- | --- | --- | --- |
  atlas_now:
    type: dataviewjs
    runtime_view: renderAtlasNow
    use_for:
      - dashboard
      - atlante
      - mappe
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderAtlasNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Scelta | Stato |
      | --- | --- |
      | Fai adesso | correggi il prossimo buco cartografico |
  atlas_readiness:
    type: dataviewjs
    runtime_view: renderAtlasReadiness
    use_for:
      - dashboard
      - atlante
      - mappe
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderAtlasReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Luoghi |  |
      | Marker |  |
      | Rotte |  |
  atlas_queues:
    type: dataviewjs
    runtime_view: renderAtlasQueues
    use_for:
      - dashboard
      - atlante
      - mappe
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderAtlasQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Azione |
      | --- | --- |
  atlas_surface_links:
    type: dataviewjs
    runtime_view: renderAtlasSurfaceLinks
    use_for:
      - dashboard
      - atlante
      - mappe
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderAtlasSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Atlante con marker | vedere coordinate e layer |
      | Importare mappe | normalizzare asset esterni |
  dnd55_material_pipeline:
    type: dataviewjs
    runtime_view: renderDnd55MaterialPipeline
    use_for:
      - session
      - dnd55
      - materiali
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderDnd55MaterialPipeline(dv);
    fallback: |-
      | Materiale | Mondo | Uso |
      | --- | --- | --- |
      | Incontri | luogo, missione, fazione, sessione | scena pronta |
      | Creature | habitat, luogo, fazione, missione | minaccia o leva |
      | Oggetti | luogo, proprietario, missione, conseguenza | ricompensa o prova |
  combat_readiness:
    type: dataviewjs
    runtime_view: renderCombatReadiness
    use_for:
      - incontro
      - dnd55
      - combattimento
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCombatReadiness(dv);
    fallback: |-
      | Incontro | Creature | Luogo | Aggancio |
      | --- | --- | --- | --- |
  dm_dashboard_now:
    type: dataviewjs
    runtime_view: renderDmDashboardNow
    use_for:
      - dashboard
      - dm
      - cockpit
      - triage
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderDmDashboardNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Fai adesso | prepara, gioca, chiudi o smista |
  dm_dashboard_readiness:
    type: dataviewjs
    runtime_view: renderDmDashboardReadiness
    use_for:
      - dashboard
      - dm
      - cockpit
      - stato
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderDmDashboardReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Sessioni |  |
      | Missioni |  |
      | Materiali |  |
      | Inbox |  |
  dm_dashboard_queues:
    type: dataviewjs
    runtime_view: renderDmDashboardQueues
    use_for:
      - dashboard
      - dm
      - cockpit
      - code
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderDmDashboardQueues(dv);
    fallback: |-
      | Coda | Prossima azione |
      | --- | --- |
  dm_dashboard_surface_links:
    type: dataviewjs
    runtime_view: renderDmDashboardSurfaceLinks
    use_for:
      - dashboard
      - dm
      - cockpit
      - navigazione
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderDmDashboardSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Durante il Gioco | giocare la sessione attiva |
      | Preparazione sessione | rifinire il prossimo tavolo |
  vault_control_now:
    type: dataviewjs
    runtime_view: renderVaultControlNow
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Ripara prima | correggi il blocco che impedisce preparazione, tavolo o post-sessione |
  vault_control_readiness:
    type: dataviewjs
    runtime_view: renderVaultControlReadiness
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Inbox |  |
      | Sessioni |  |
      | Materiali |  |
      | Coerenza |  |
  vault_control_queues:
    type: dataviewjs
    runtime_view: renderVaultControlQueues
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlQueues(dv);
    fallback: |-
      | Nota | Azione |
      | --- | --- |
  vault_control_coherence:
    type: dataviewjs
    runtime_view: renderVaultControlCoherence
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - audit
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlCoherence(dv);
    fallback: |-
      | Controllo | Problema |
      | --- | --- |
  vault_control_surface_links:
    type: dataviewjs
    runtime_view: renderVaultControlSurfaceLinks
    use_for:
      - dashboard
      - controllo vault
      - manutenzione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderVaultControlSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Quality report | vedere problemi ampi |
      | Smistamento bozze | decidere materiale generato |
  worldbuilder_now:
    type: dataviewjs
    runtime_view: renderWorldbuilderNow
    use_for:
      - dashboard
      - worldbuilding
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldbuilderNow(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Scelta | Stato |
      | --- | --- |
      | Fai adesso | crea o collega il prossimo elemento vivo |
  worldbuilder_readiness:
    type: dataviewjs
    runtime_view: renderWorldbuilderReadiness
    use_for:
      - dashboard
      - worldbuilding
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldbuilderReadiness(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Mancano |  |
      | Pronte |  |
      | Pressioni |  |
  worldbuilder_queues:
    type: dataviewjs
    runtime_view: renderWorldbuilderQueues
    use_for:
      - dashboard
      - worldbuilding
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderWorldbuilderQueues(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Nota | Azione |
      | --- | --- |
  worldbuilder_surface_links:
    type: dataviewjs
    runtime_view: renderWorldbuilderSurfaceLinks
    use_for:
      - dashboard
      - worldbuilding
      - cockpit
      - mappe
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderWorldbuilderSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Codex editabile | correggere stati e collegamenti |
      | Atlante con marker | vedere coordinate e layer |
  worldbuilding_control_now:
    type: dataviewjs
    runtime_view: renderWorldbuildingControlNow
    use_for:
      - dashboard
      - worldbuilding
      - controllo
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldbuildingControlNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Ripara prima | controlla profondita, connessioni, canone e player-safe |
  worldbuilding_control_readiness:
    type: dataviewjs
    runtime_view: renderWorldbuildingControlReadiness
    use_for:
      - dashboard
      - worldbuilding
      - controllo
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderWorldbuildingControlReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Profondita |  |
      | Connessioni |  |
      | Canone |  |
      | Player-safe |  |
  worldbuilding_control_queues:
    type: dataviewjs
    runtime_view: renderWorldbuildingControlQueues
    use_for:
      - dashboard
      - worldbuilding
      - controllo
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderWorldbuildingControlQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Riparazione |
      | --- | --- |
  worldbuilding_control_surface_links:
    type: dataviewjs
    runtime_view: renderWorldbuildingControlSurfaceLinks
    use_for:
      - dashboard
      - worldbuilding
      - controllo
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderWorldbuildingControlSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Codex editabile | correggere campi e connessioni |
      | Controllo canone | stabilizzare verita e rumor |
  canon_control_now:
    type: dataviewjs
    runtime_view: renderCanonControlNow
    use_for:
      - dashboard
      - canone
      - lore
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCanonControlNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Sistema prima | controlla fonte, rumor, contraddizioni e retcon |
  canon_control_readiness:
    type: dataviewjs
    runtime_view: renderCanonControlReadiness
    use_for:
      - dashboard
      - canone
      - lore
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderCanonControlReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Canonico |  |
      | Rumor |  |
      | Contraddizioni |  |
      | Retcon |  |
  canon_control_queues:
    type: dataviewjs
    runtime_view: renderCanonControlQueues
    use_for:
      - dashboard
      - canone
      - lore
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderCanonControlQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Decisione |
      | --- | --- |
  canon_control_surface_links:
    type: dataviewjs
    runtime_view: renderCanonControlSurfaceLinks
    use_for:
      - dashboard
      - canone
      - lore
      - cockpit
      - audit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderCanonControlSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Lore hub | vedere segnali e materiale collegato |
      | Revisione lore | ripulire appunti e rumor |
  economy_now:
    type: dataviewjs
    runtime_view: renderEconomyNow
    use_for:
      - dashboard
      - economia
      - rotte
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderEconomyNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Muovi prima | scegli rotta, risorsa, mercato o buco economico |
  economy_readiness:
    type: dataviewjs
    runtime_view: renderEconomyReadiness
    use_for:
      - dashboard
      - economia
      - rotte
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderEconomyReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Rotte |  |
      | Risorse |  |
      | Mercati |  |
      | Buchi |  |
  economy_queues:
    type: dataviewjs
    runtime_view: renderEconomyQueues
    use_for:
      - dashboard
      - economia
      - rotte
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderEconomyQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nodo | Stato |
      | --- | --- |
  economy_dependency_queues:
    type: dataviewjs
    runtime_view: renderEconomyDependencyQueues
    use_for:
      - dashboard
      - economia
      - rotte
      - dipendenze
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderEconomyDependencyQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Problema |
      | --- | --- |
  economy_surface_links:
    type: dataviewjs
    runtime_view: renderEconomySurfaceLinks
    use_for:
      - dashboard
      - economia
      - rotte
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderEconomySurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Geopolitica | leggere confini e crisi |
      | Motore Mondo Vivo | propagare conseguenze |
  lore_now:
    type: dataviewjs
    runtime_view: renderLoreNow
    use_for:
      - dashboard
      - lore
      - canone
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLoreNow(dv, dv.current().mondo_attivo);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Segnale prima | scegli canone, mistero, evento o lore da rendere giocabile |
  lore_readiness:
    type: dataviewjs
    runtime_view: renderLoreReadiness
    use_for:
      - dashboard
      - lore
      - canone
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLoreReadiness(dv, dv.current().mondo_attivo);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Culture |  |
      | Storia |  |
      | Misteri |  |
      | Materiali |  |
  lore_signal_queues:
    type: dataviewjs
    runtime_view: renderLoreSignalQueues
    use_for:
      - dashboard
      - lore
      - canone
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLoreSignalQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Nota | Decisione |
      | --- | --- |
  lore_world_queues:
    type: dataviewjs
    runtime_view: renderLoreWorldQueues
    use_for:
      - dashboard
      - lore
      - worldbuilding
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLoreWorldQueues(dv, dv.current().mondo_attivo);
    fallback: |-
      | Materiale | Uso |
      | --- | --- |
  lore_surface_links:
    type: dataviewjs
    runtime_view: renderLoreSurfaceLinks
    use_for:
      - dashboard
      - lore
      - canone
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLoreSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Atlante | collegare luoghi e mappe |
      | Controllo canone | decidere verita e rumor |
  living_world_now:
    type: dataviewjs
    runtime_view: renderLivingWorldNow
    use_for:
      - dashboard
      - mondo vivo
      - propagazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLivingWorldNow(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Cambia prima | scegli conseguenza, bersaglio o pressione da aggiornare |
  living_world_readiness:
    type: dataviewjs
    runtime_view: renderLivingWorldReadiness
    use_for:
      - dashboard
      - mondo vivo
      - propagazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLivingWorldReadiness(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Canone vivo |  |
      | Pressioni |  |
      | Propagazioni |  |
      | Buchi |  |
  living_world_queues:
    type: dataviewjs
    runtime_view: renderLivingWorldQueues
    use_for:
      - dashboard
      - mondo vivo
      - propagazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLivingWorldQueues(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Nota | Azione |
      | --- | --- |
  living_world_pressure_queues:
    type: dataviewjs
    runtime_view: renderLivingWorldPressureQueues
    use_for:
      - dashboard
      - mondo vivo
      - pressioni
      - economia
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLivingWorldPressureQueues(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Nodo | Pressione |
      | --- | --- |
  living_world_surface_links:
    type: dataviewjs
    runtime_view: renderLivingWorldSurfaceLinks
    use_for:
      - dashboard
      - mondo vivo
      - propagazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLivingWorldSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Fuori scena | scegliere reazioni tra sessioni |
      | Controllo canone | stabilizzare verita e retcon |
  offscreen_now:
    type: dataviewjs
    runtime_view: renderOffscreenNow
    use_for:
      - dashboard
      - fuori scena
      - reazioni
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderOffscreenNow(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Reagisce prima | scegli attore, clock o conseguenza da muovere |
  offscreen_readiness:
    type: dataviewjs
    runtime_view: renderOffscreenReadiness
    use_for:
      - dashboard
      - fuori scena
      - reazioni
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderOffscreenReadiness(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Attori in moto |  |
      | Clock urgenti |  |
      | Conseguenze aperte |  |
      | Ponte al tavolo |  |
  offscreen_reaction_queues:
    type: dataviewjs
    runtime_view: renderOffscreenReactionQueues
    use_for:
      - dashboard
      - fuori scena
      - reazioni
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderOffscreenReactionQueues(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Nota | Reazione |
      | --- | --- |
  offscreen_table_bridge:
    type: dataviewjs
    runtime_view: renderOffscreenTableBridge
    use_for:
      - dashboard
      - fuori scena
      - preparazione
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderOffscreenTableBridge(dv, dv.current().mondo_attivo, dv.current().campagne_attive);
    fallback: |-
      | Materiale | Uso |
      | --- | --- |
  offscreen_surface_links:
    type: dataviewjs
    runtime_view: renderOffscreenSurfaceLinks
    use_for:
      - dashboard
      - fuori scena
      - reazioni
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderOffscreenSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Motore mondo vivo | verificare propagazioni |
      | Preparazione sessione | portare la reazione al tavolo |
  live_table_now:
    type: dataviewjs
    runtime_view: renderLiveTableNow
    use_for:
      - dashboard
      - durante il gioco
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLiveTableNow(dv);
    fallback: |-
      | Priorita | Azione |
      | --- | --- |
      | Gioca adesso | usa scena, scelta, pressione e cattura live |
  live_table_readiness:
    type: dataviewjs
    runtime_view: renderLiveTableReadiness
    use_for:
      - dashboard
      - durante il gioco
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      gdr.renderLiveTableReadiness(dv);
    fallback: |-
      | Area | Conteggio |
      | --- | --- |
      | Luoghi |  |
      | Persone |  |
      | Missioni |  |
      | Materiali |  |
  live_table_queues:
    type: dataviewjs
    runtime_view: renderLiveTableQueues
    use_for:
      - dashboard
      - durante il gioco
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLiveTableQueues(dv);
    fallback: |-
      | Nota | Uso |
      | --- | --- |
  live_table_materials:
    type: dataviewjs
    runtime_view: renderLiveTableMaterials
    use_for:
      - dashboard
      - durante il gioco
      - materiali
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLiveTableMaterials(dv);
    fallback: |-
      | Materiale | Stato |
      | --- | --- |
  live_table_surface_links:
    type: dataviewjs
    runtime_view: renderLiveTableSurfaceLinks
    use_for:
      - dashboard
      - durante il gioco
      - tavolo
      - cockpit
    code: |-
      const gdr = await eval(await app.vault.adapter.read("z.engine/session_views.js"));
      await gdr.renderLiveTableSurfaceLinks(dv);
    fallback: |-
      | Superficie | Uso |
      | --- | --- |
      | Party control | controllare PG e risorse |
      | Iniziativa | gestire scene a turni |
contracts:
  dataviewjs_has_no_file_writes: true
  empty_state_required: true
  fallback_required: true
