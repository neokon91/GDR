{% import '_macros.j2' as m with context -%}
<% await tp.user.create_entity(tp, "{{ template.id }}") %>
# `=this.nome`

````tabs
--- Scena

> [!danger] Uso al tavolo
> {{ m.field('uso_al_tavolo', label=false) }}

> [!warning]- Pericolo
> {{ m.field('pressione') }}
>
> {{ m.field('prossima_mossa') }}

--- Combattimento

```encounter
# Aggiungi creature per nome dal bestiario, es. "3: Goblin".
creatures:
  - 1: Goblin
```

> [!tip]- Tiri
> `dice: 1d20`

--- Collegamenti

> [!example] Ancore
> {{ m.field('mondo') }}
>
> {{ m.field('connessioni') }}

--- Vista

```dataviewjs
const source = await dv.io.load("z.automazioni/views.js");
eval(source);
renderEntityPanel(dv, dv.current());
```
````
