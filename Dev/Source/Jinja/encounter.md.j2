{% import '_macros.j2' as m with context -%}
<% await tp.user.create_entity(tp, "{{ template.id }}") %>
# `=this.nome`

````tabs
tab: Scena

> [!incontro] Uso al tavolo
> {{ m.field('uso_al_tavolo', label=false) }}

> [!pericolo]- Pericolo
> {{ m.field('pressione') }}
>
> {{ m.field('prossima_mossa') }}

tab: Creature

```dataview
list from [[]] where categoria = "creatura"
```

tab: Collegamenti

> [!luogo] Ancore
> {{ m.field('mondo') }}
>
> {{ m.field('connessioni') }}

tab: Vista

```dataviewjs
const source = await dv.io.load("z.automazioni/views.js");
eval(source);
renderEntityPanel(dv, dv.current());
```
````
