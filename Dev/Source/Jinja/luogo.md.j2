{# Luogo — template per-entità. Estende lo scheletro condiviso e personalizza la
   tab Lore (identità del luogo + body arricchito da FantasyWorld t.luogo.yaml).
   Tutto il resto (tavolo/carattere/collegamenti/vista) viene da _entity_base.j2;
   lo schema è in YAML/entities/luogo.yaml. #}
{% extends '_entity_base.j2' %}
{% block lore %}
{% import '_macros.j2' as m with context %}
{{ m.scheda(template.category) }}
{{ m.genera_nome() }}
> [!note]- Colpo d'occhio
> Cos'è il luogo, che impressione dà entrandoci, perché conta nella storia.

> [!quote]- Versione player-safe
> {{ m.field('player_safe', label=false) }}

{{ m.wizard_body(template.category) }}
{% endblock %}
{% block extra_tabs %}
{% import '_macros.j2' as m with context %}
--- 🧭 Spazio

{{ m.mappa() }}

> [!info] Posizione
> Coordinate sulla mappa: {{ m.field('coord', label=false) }} — `x, y` (per la distanza in linea d'aria; la scala è sul **Mondo**).

{{ m.dintorni() }}

{{ m.viaggio() }}
{% endblock %}
